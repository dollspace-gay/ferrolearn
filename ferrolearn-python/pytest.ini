[pytest]
# Discover both the house `test_*.py` suite and the ACToR `divergence_*.py`
# guard files (goal.md verification model B mandates the divergence_<module>.py
# naming for pinned/guard tests).
python_files = test_*.py divergence_*.py
